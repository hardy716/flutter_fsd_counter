name: flutter_fsd_counter
description: "flutter display application with FSD(Feature-Sliced Design)"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  flutter_riverpod:
  go_router:
  dio:
  freezed_annotation:
  json_annotation:


dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner:
  freezed:
  json_serializable:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
